/*
 * BooksPage Messages
 *
 * This contains all the text for the ProductsList container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.pages.ProductsList'

export default defineMessages({
  header: {
    id: `${scope}.search`,
    defaultMessage: 'Search',
  },
})
