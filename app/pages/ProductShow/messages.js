/*
 * ProductShow Messages
 *
 * This contains all the text for the ProductShow container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.pages.ProductShow'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductShow container!',
  },
})
