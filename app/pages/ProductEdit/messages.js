/*
 * ProductEdit Messages
 *
 * This contains all the text for the ProductEdit container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.pages.ProductEdit'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductEdit container!',
  },
})
