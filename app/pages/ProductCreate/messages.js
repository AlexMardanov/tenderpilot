/*
 * ProductCreate Messages
 *
 * This contains all the text for the ProductCreate page.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.pages.ProductCreate'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductCreate container!',
  },
})
